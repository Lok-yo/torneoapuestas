# Tasks: Production-ready tournament betting platform — Stage 1

## Resolved Decisions Baked Into This Plan

- **Launch game/format** (was blocking): `ssbu` (id matches `src/data/games.js`), 1v1 singles, single-elimination, 8-participant roster, best-of-3 (first to 2) — see 3.1/3.8/3.11. Domain persists the official result only; it never simulates or validates in-game play.
- **First admin bootstrap** (was blocking): one-time Supabase SQL migration granting `admin` to the fixed email `lleonalmaza@gmail.com`, applied by the maintainer with the service-role key — see 1.2. No runtime endpoint, JWT-claim grant, or env-var allowlist.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~6,000–10,000+ (15+ migrations, 4 Edge Functions, domain/repository/UI code, 3 test layers, CI) |
| Project review budget (`openspec/config.yaml`) | 1,000 lines (`review_budget_lines`) — estimate exceeds it |
| 400-line budget risk | High |
| Chained PRs recommended | Yes (technically); config pins `delivery_strategy: single-pr` |
| Suggested split | 8 work units (below), usable as internal review checkpoints regardless of PR count |
| Delivery strategy | single-pr |
| Chain strategy | size-exception |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: size-exception
400-line budget risk: High

Rationale: `delivery_strategy: single-pr` in `openspec/config.yaml` requires maintainer-approved `size:exception` before `sdd-apply` proceeds, since the estimate exceeds both the 400-line default and the project's configured 1,000-line budget.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | Test tooling + Supabase scaffold (Phase 0) | slice 1 | `npm run test -- --run` | `supabase init && supabase db reset` (local) | Remove config files only; no behavior change |
| 2 | Schema/RLS/admin-bootstrap (Phase 1) | slice 2 | `supabase test db` | `supabase db reset` against local Postgres | Revert migrations 0001-0005 via down migrations |
| 3 | Identity (Phase 2) | slice 3 | `npm run test -- src/auth` | `supabase functions serve bootstrap-session` + Playwright OAuth stub | Disable identity flag; Edge Function removable independently |
| 4 | Tournament operations (Phase 3) | slice 4 | `npm run test -- src/domain/tournaments` | `supabase functions serve result-command` + Playwright tournament-flow | Disable tournaments flag; RPCs additive/revertible |
| 5 | Ratings (Phase 4) | slice 5 | `npm run test -- src/domain/ratings` | `npx playwright test e2e/leaderboard-history.spec.js` | Disable ratings flag; trigger/RPC additive |
| 6 | Legacy migration controls (Phase 5) | slice 6 | `npm run test -- src/config/featureFlags` | `npx playwright test e2e/no-financial-ui.spec.js` | Flags default off; fixtures untouched |
| 7 | Routing threat matrix (Phase 6) | slice 7 | `npx playwright test e2e/session-routing.spec.js` | Playwright against staging preview | Revert `RequireAuth.jsx`/guard changes only |
| 8 | CI, docs, cleanup (Phase 7) | slice 8 | `npm run lint && npm run build` | Full CI pipeline run | Revert workflow/docs file only |

## Phase 0: Tooling & Test Harness Bootstrap

- [x] 0.1 Add Vitest + RTL: `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom` in `package.json`; add `vitest.config.js`; add `npm run test` script. — Evidence: `npx vitest run` → 2 files, 10/10 passed.
- [x] 0.2 Add Playwright: `@playwright/test` devDep, `playwright.config.js`, `npm run test:e2e` script. — Evidence: `npx playwright test --list` loads config with 0 spec files (none written yet).
- [x] 0.3 Add Supabase CLI scaffold: `supabase/config.toml`, `.env.example` with `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY` placeholders (no real secrets). — `supabase/config.toml` created (batch 1). `.env.example` created by the maintainer directly at the repo root, outside the sandbox's `.env*` deny-list (neither the apply agent nor the orchestrator could read/write it themselves — confirmed independently by both). Content verified by the maintainer directly against the environment contract documented in `README.md` "Environment and secrets contract" (batch 5, task 7.2) and confirmed to the orchestrator on 2026-08-12.
- [x] 0.4 Add Postgres test harness: `supabase/tests/` pgTAP setup, `npm run test:db` wrapping `supabase test db`. — Evidence: `supabase/tests/README.md` + `0000_extensions.sql` (pgtap/pgcrypto); 2 real pgTAP suites (1.6, 1.8) pass against a live container.
- [x] 0.5 Update `.github/workflows/ci.yml`: frozen install, lint, unit test, migration replay, pgTAP, build, Playwright jobs (fail closed). — Jobs: install → {lint, unit-test, db-migrations} → build → e2e; audit job also added (7.1 will finalize ordering/`npm audit` gate).

## Phase 1: Platform Foundation — Schema, Grants, RLS

- [x] 1.1 Migration `0001_roles_and_grants.sql`: `role` enum (`user`,`organizer`,`referee`,`admin`), `user_roles` table keyed by `auth.uid()`, default `user`. — Applied cleanly to `supabase/postgres:15.8.1.049`.
- [x] 1.2 Migration `0002_admin_bootstrap.sql`: idempotent insert granting `admin` to fixed email `lleonalmaza@gmail.com` via `auth.users.email` lookup, upsert-on-conflict-do-nothing; applied by maintainer with service-role key at deploy; no runtime/browser grant path. — Applied cleanly (0 rows on empty target, as expected).
- [x] 1.3 Migration `0003_core_tables.sql`: `profiles`, `games`, `tournament_formats`, `tournaments`, `memberships`, `brackets`, `matches`, `results`, `rating_events`, `rating_snapshots`, `audit_events`, `command_outcomes` (idempotency ledger). — Applied cleanly.
- [x] 1.4 Migration `0004_rls_policies.sql`: enable RLS on all tables above, deny-by-default, owner/role-scoped policies. — Applied cleanly; explicit REVOKE-then-GRANT plus RLS policies (defense-in-depth) on every table.
- [x] 1.5 Migration `0005_public_views.sql`: `public_tournaments_view`, `public_brackets_view`, `public_leaderboard_view`, `public_player_history_view` — allowlisted fields only. — Applied cleanly; `security_invoker = true` so views cannot widen access beyond base-table RLS.
- [x] 1.6 RED pgTAP `supabase/tests/rls_deny_by_default.sql`: assert `anon`/other-user access to profiles/memberships/audit_events/command_outcomes is denied. — 8/8 `ok` against the live container.
- [x] 1.7 GREEN: fix policy gaps in 1.4 until 1.6 passes. — No gaps found; 1.6 passed 8/8 on first run against the implemented 0004/0005 migrations.
- [x] 1.8 RED pgTAP `supabase/tests/admin_bootstrap.sql`: assert only the fixed email holds `admin`. — 4/4 `ok`, including a replay-idempotency assertion.
- [x] 1.9 Shared structured-error helper `supabase/functions/_shared/errors.ts` and `src/lib/errors.js`: `{ error: { code, message, requestId, retryable, details? } }` mapped to 400/401/403/404/409/422/429/503. — `src/lib/errors.js` covered by `src/lib/__tests__/errors.test.js` (6/6 pass).
- [x] 1.10 Observability scaffold `supabase/functions/_shared/log.js`: correlated structured logs, request-id propagation, no secrets/PII. — Covered by `supabase/functions/_shared/__tests__/log.test.js` (4/4 pass); denylist-based redaction.
- [x] 1.11 CI migration-replay gate (extends 0.5): `supabase db reset` succeeds on a clean target from committed migrations only. — `db-migrations` CI job added; migration ordering/idempotency manually verified via sequential `psql -f` replay of 0000-0005 against a clean container (functionally equivalent to `supabase db reset`).

## Phase 2: Authenticated Identity

- [x] 2.1 RED unit `src/auth/__tests__/SessionProvider.test.jsx`: anonymous/expired/loading/authenticated states. — Confirmed RED (import of not-yet-existing `SessionProvider.jsx` failed), then GREEN: `npx vitest run src/auth/__tests__/SessionProvider.test.jsx` 5/5 pass.
- [x] 2.2 Edge Function `supabase/functions/bootstrap-session/index.ts`: validate JWT, idempotent profile upsert, return profile+role. — Forwards caller's own JWT so Postgres RLS (not the function) authorizes every read/write; `roles` returned as an array of grants. No Deno runtime available in this environment to execute it directly (same constraint noted for `_shared/errors.ts` in batch 1); verified by code review against the same `errors.ts`/`log.js` conventions and by its client-side counterpart (`sessionRepository.bootstrapSession`) being exercised through the Playwright e2e suite via network-level stubbing of this exact endpoint.
- [x] 2.3 Migration `0006_username_claim.sql`: case-insensitive unique index on `profiles.username_normalized`, `username_claim_attempts` throttle table. — Applied cleanly (0000-0006 sequential replay) against `supabase/postgres:15.8.1.049`.
- [x] 2.4 RPC `claim_username(request_id, username)` (same migration): normalize/validate, atomic unique insert, idempotent replay on same `request_id`, throttled past threshold. — Returns a structured `{status: 'claimed'|'conflict'|'rate_limited'}` result (never raises for these three expected outcomes, so throttle/audit bookkeeping is never rolled back by a later exception in the same call); only raises for UNAUTHENTICATED (28000) and malformed-request/format VALIDATION_ERROR (22023).
- [x] 2.5 RED pgTAP `supabase/tests/username_claim_race.sql`: case-variant claims → one succeeds, one stable conflict; idempotent replay; throttle. — Initial run: 2/11 failed (RED, caught two real bugs: `reset role` doesn't clear a `set local` GUC, and a later `raise` was silently rolling back an earlier `insert` within the same call). Fixed in 0006; rerun: 11/11 `ok`. True two-backend OS-level concurrency is explicitly out of scope for one pgTAP transaction (documented in the test file); the unique index is what actually makes concurrent races safe regardless of arrival order.
- [x] 2.6 GREEN: adjust locking/constraint in 2.4 until 2.5 passes. — Restructured `claim_username` so format validation happens before any recorded state, and business outcomes (claimed/conflict/rate_limited) are always returned, never raised, eliminating the savepoint-rollback bug found by 2.5. 11/11 pass; `rls_deny_by_default` (8/8) and `admin_bootstrap` (4/4) re-run with zero regressions.
- [x] 2.7 `src/lib/supabase.js`, `src/repositories/sessionRepository.js`, `src/repositories/profileRepository.js`. — Added `@supabase/supabase-js` dependency. `isSupabaseConfigured` fails into a truthful unavailable state when env vars are missing rather than throwing inside the client constructor.
- [x] 2.8 `src/auth/SessionProvider.jsx`, `src/components/RequireAuth.jsx` (role/ownership gate); wire into `src/App.jsx`/`src/main.jsx`. — States: `loading`/`anonymous`/`authenticated`/`expired` (expired = recoverable auth-state per spec, protected commands denied same as anonymous). `RequireAuth` now accepts optional `role`/`ownerId` props (client-side UX gate only; RLS/grants remain the real authority). Added `OnboardingRedirect` in `App.jsx`: since a real Google OAuth redirect returns the browser to `/`, not wherever "sign in" was clicked, this globally redirects any authenticated-but-no-username session to `/onboarding` from any route.
- [x] 2.9 `src/pages/LoginPage.jsx`, `src/pages/OnboardingUsernamePage.jsx`: Google sign-in trigger, claim form, error surfacing. — Onboarding form calls `claimUsername` and surfaces `conflict`/`rate_limited`/validation states distinctly (Spanish user-facing copy, matching project convention).
- [x] 2.10 RED Playwright `e2e/auth-onboarding.spec.js`: OAuth-stub sign-in → onboarding → collision/throttle → protected-route access. — Stub technique: navigating to `/#access_token=...` is byte-for-byte what Supabase Auth's implicit-flow redirect back from Google produces; `GET /auth/v1/user`, `POST /functions/v1/bootstrap-session`, and `POST /rest/v1/rpc/claim_username` are intercepted with `page.route()` fixtures — no real Google/Supabase network call is ever made. First run against a real `npm run build` + `npm run preview` (with stub, non-secret `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`) caught a real bug (see 2.11); after the fix, RED→GREEN.
- [x] 2.11 GREEN: complete wiring until 2.1/2.10 pass. — The e2e RED run found that landing on `/` with a fresh authenticated-but-no-username session never redirected to onboarding (only `LoginPage`'s own post-click flow did, which a real full-page OAuth redirect bypasses). Fixed via the `OnboardingRedirect` in 2.8. `npx playwright test e2e/auth-onboarding.spec.js`: 5/5 pass. Also fixed a latent CI gap while wiring this in: `vitest.config.js` now excludes `e2e/**` (Playwright's own `test`/`describe` globals conflicted with Vitest's default include glob once a real spec file existed), and `.github/workflows/ci.yml`'s `e2e` job now builds with stub Supabase env vars before `npx playwright test` (previously it ran `playwright test` — which serves `dist/` via `npm run preview` — without ever building `dist/`, a gap invisible while there were 0 spec files).

## Phase 3: Tournament Operations (SSBU, single-elimination, 8 participants)

- [x] 3.1 `supabase/seed.sql` + migration `0007_ssbu_format_seed.sql`: one `games` row `ssbu`; one `tournament_formats` row — singles 1v1, single-elimination, `roster_size=8`, `best_of=3` (first to 2), ruleset metadata (3 stocks, 7-minute clock, items off, stage striking/counterpick) as versioned JSON. — Idempotent (`on conflict do nothing`); `seed.sql` documents that Stage 1's catalog is a migration, not a dev-only fixture.
- [x] 3.2 RED unit `src/domain/tournaments/__tests__/lifecycle.test.js`: `DRAFT→REGISTRATION_OPEN→REGISTRATION_CLOSED→IN_PROGRESS→COMPLETED` + cancellation; invalid transitions rejected. — Confirmed RED (import failure), then GREEN: 10/10 pass.
- [x] 3.3 `src/domain/tournaments/lifecycle.js` + RPC `advance_tournament_state` in `0008_lifecycle_rpc.sql`: organizer-only, versioned/audited, rejects invalid state/format. — pgTAP `lifecycle_rpc.sql` 8/8 (unauthorized denial, happy path, version-conflict, invalid-transition, freeze-on-close, idempotent replay).
- [x] 3.4 RED unit: eligibility, unique membership, pre-freeze withdrawal, frozen-roster rejection. — `src/domain/tournaments/__tests__/registration.test.js`, confirmed RED then GREEN: 8/8 pass.
- [x] 3.5 RPCs `register_participant`/`withdraw_participant` in `0009_registration_rpc.sql`: enforce eligibility/uniqueness/freeze, retry-safe. — Locks the tournament row before reading membership state; idempotent by `request_id`; re-registration reuses the same membership row via `on conflict`.
- [x] 3.6 RED pgTAP `supabase/tests/roster_concurrency.sql`: concurrent duplicate registration → one membership. — 13/13 pass (duplicate rejection, withdrawal+re-registration reuse, 8-participant capacity, roster-full rejection, freeze rejection). True 2-backend concurrency documented as out of scope for one pgTAP transaction (same limitation as `username_claim_race.sql`); the `for update` lock is the actual safety mechanism.
- [x] 3.7 RED unit: deterministic bracket for 8 seeds, no fabricated winners. — `src/domain/tournaments/__tests__/bracket.test.js`, confirmed RED then GREEN: 6/6 pass.
- [x] 3.8 RPC `generate_bracket` in `0010_bracket_rpc.sql`: single-elimination graph for `roster_size=8`, one transaction, replays prior `command_outcomes` on retry. — Real bug found+fixed during pgTAP verification: FK insert ordering (round 1 referenced round-2/3 match ids before those rows existed); fixed by inserting leaf-to-root (final → round 2 → round 1).
- [x] 3.9 RED pgTAP `supabase/tests/bracket_concurrency.sql`: concurrent/repeated generation → one equivalent bracket, no partial graph. — 11/11 pass (unauthorized denial, correct 7-match graph, all 8 seeds assigned exactly once, zero fabricated round-2+ participants, idempotent replay, repeated-call "already_exists" path).
- [x] 3.10 RED unit: result authority, `games_won_a`/`games_won_b` score schema (best-of-3, e.g. 2-0/2-1), stale/repeat rejection. — `src/domain/tournaments/__tests__/result.test.js`, confirmed RED then GREEN: 10/10 pass.
- [x] 3.11 Edge Function `supabase/functions/result-command/index.ts` + RPC `submit_official_result` in `0011_result_rpc.sql`: organizer/referee-only, validates score against `best_of=3` schema, atomic result+advance+audit, idempotent replay. — pgTAP `result_submission.sql` 9/9 (unauthorized denial, invalid score, valid 2-0 acceptance, winner propagated into next match without fabrication, repeat-on-completed rejection, idempotent replay). Edge Function itself not directly executable in this environment (no Deno runtime, same constraint as batches 1-2's `bootstrap-session`); verified by code review against `_shared/errors.ts`/`log.js` conventions and exercised end-to-end via the Playwright e2e route stub.
- [x] 3.12 `src/repositories/tournamentRepository.js`, `bracketRepository.js`, `resultRepository.js`. — Deviation from the original plan, documented below: reads use the `tournaments` base table directly (not a view) since 0004's RLS already scopes it correctly (published + own-drafts) and includes `organizer_id`/`version`, which the UI's lifecycle controls need.
- [x] 3.13 `src/pages/TournamentsPage.jsx`, `src/pages/TournamentDetailPage.jsx`: async states, repository wiring, role-gated result form. — Rewritten against real repositories (loading/error/empty/not-found states); new `src/components/TournamentStatusBadge.jsx` for the real lifecycle enum (kept separate from the legacy mock `StatusBadge.jsx`, still used by `HomePage`/`MarketDetailPage`).
- [x] 3.14 RED Playwright `e2e/tournament-flow.spec.js`: registration→freeze→bracket→organizer result→public projection, plus outage truthful-unavailable state. — 2/2 pass. Real bug found+fixed: a `route.continue()` guard for non-GET methods forwarded the CORS preflight to the fake stub host and hung; removed. Also discovered supabase-js's built-in retry-with-backoff on a 503 GET takes several seconds — the outage assertion's timeout was extended (20s) to match that real, correct resilience behavior rather than treating it as a bug.
- [x] 3.15 GREEN: complete wiring until 3.2/3.4/3.7/3.10/3.14 pass. — All five green: 10/10, 8/8, 6/6, 10/10, 2/2 respectively.
- [x] 3.16 (unplanned, found during 3.12/3.13 wiring) Migration fix in `0004_rls_policies.sql`/`0005_public_views.sql`: `public_brackets_view`/`public_leaderboard_view`/`public_player_history_view` were `security_invoker = true` views joining RLS-private tables (`memberships`, `profiles`, `rating_events`, `rating_snapshots`) that `anon` has zero grant on — an anonymous reader got a permission error, not the published projection, silently failing the tournament-operations "Public projection and closed perimeter" requirement from Phase 1. Fixed by removing `security_invoker` (views now run as their owner, the same "bypass RLS, expose only the curated columns" pattern already used by every SECURITY DEFINER RPC) rather than granting `anon` direct table access, which would have violated 0004's own "public reads go through the views, never these base tables" convention. New pgTAP `supabase/tests/public_projection.sql` (7/7) proves anon can read a published tournament's bracket/score and cannot read a DRAFT tournament's bracket or any private table.

## Phase 4: Rating Projections

- [x] 4.1 RED unit `src/domain/ratings/__tests__/projector.test.js`: official result → versioned event+snapshot; simulated/draft input produces none. — Confirmed RED (import failure against not-yet-existing `../projector.js`), then GREEN: `npx vitest run src/domain/ratings/__tests__/projector.test.js` 9/9 pass.
- [x] 4.2 Migration `0012_rating_projector_trigger.sql`: trigger/RPC on accepted result → append `rating_events`, recompute `rating_snapshots` deterministically. — `AFTER INSERT ON results` trigger (fires for both original submission and correction inserts, one projector, not duplicated); `recompute_rating_snapshot` is a deterministic full recompute from retained CLEAN events tied to the currently-OFFICIAL result, base 1000 + WIN_DELTA(25)/LOSE_DELTA(-25).
- [x] 4.3 RED unit: repeated delivery → one equivalent event; conflicting source versions → marked for review, not silently resolved. — Same file/run as 4.1 (`resolveCorrectionOutcome` version-conflict tests + repeated-projection equivalence test), 9/9 pass.
- [x] 4.4 Implement idempotency/conflict handling in 4.2 via `command_outcomes` + review-state column. — Idempotency: real retry-safety comes from the parent RPC's (submit_official_result/correct_result) own `command_outcomes` ledger, which prevents a retried request_id from inserting a second `results` row at all (proven by pgTAP: replay produces 0 duplicate rating_events); the trigger itself also carries a defense-in-depth `unique (result_id, participant_membership_id)` constraint + `on conflict do nothing`. Conflict handling: `review_state` (already on `rating_events` from Phase 1) is flipped to `NEEDS_REVIEW` by `correct_result` on a stale `expected_version`, without touching deltas/snapshots — existing evidence preserved, nothing silently resolved.
- [x] 4.5 RPC `correct_result` in `0013_correction_rpc.sql`: authorized actor+reason required, appends new version, recomputes, preserves prior audit. — Organizer/referee/admin-only + non-empty reason; supersedes the original row (`status='CORRECTED'`, original evidence unchanged) and inserts a new versioned `OFFICIAL` row, which the 0012 trigger projects automatically. Documented Stage-1 scope note: corrects the result/rating projection only, does not retroactively reflow already-advanced bracket state (out of this change's scope).
- [x] 4.6 pgTAP: unauthorized/reason-less correction rejected; authorized correction recomputes and preserves history. — New `supabase/tests/rating_projection.sql` (16 assertions: projection, idempotent replay, unauthorized denial, reason-less rejection, version-conflict review-marking, authorized correction+history preservation), written alongside 0012/0013 in this batch (not a strict pre-implementation RED — deviation noted honestly) and verified GREEN: 16/16 `ok` against a live `supabase/postgres:15.8.1.049` container (migrations 0000-0013 replayed cleanly), plus full regression of all 8 prior pgTAP suites (87/87 total, zero regressions). Two real test-authoring bugs were caught and fixed during verification: an RLS-restricted subquery returned null result_id for the unauthorized-bystander probe (fixed by capturing the id as the superuser test-runner via `\gset` before switching role), and psql does not interpolate `:'variables'` inside a `$$ ... $$` dollar-quoted string (fixed with `format()`/`%L` built outside the quoting).
- [x] 4.7 `src/repositories/ratingRepository.js`; `src/pages/LeaderboardPage.jsx`, `src/pages/PlayerProfilePage.jsx` wired to public views. — `getLeaderboard`/`getPlayerRatings`/`getPlayerHistory` read `public_leaderboard_view`/`public_player_history_view` directly (no fixture fallback), matching the loading/error/empty async-state pattern from `TournamentsPage.jsx`. Removed a duplicate, unused `getPublicPlayerHistory` from `profileRepository.js` (dead scaffold from an earlier phase) in favor of this single canonical rating-reads location. Also added `correctResult` to `resultRepository.js` (direct RPC call, same pattern as `advanceTournamentState`) so `correct_result` (4.5) has a client-side caller, even though no correction UI is in this batch's scope. `npm run lint` clean; `npm run build` succeeds with stub Supabase env vars; `npx vitest run` 58/58 pass (58 = 49 from batches 1-3 + 9 new domain tests).
- [x] 4.8 RED Playwright `e2e/leaderboard-history.spec.js`: leaderboard/history reflect result, versioned freshness, stale-dependency truthful state. — Deviation noted honestly (same as 4.6): written alongside the already-implemented pages rather than strictly before them, since Phase 4's pages/repository were built together in this batch. 3 tests: leaderboard ordering/rating display, player-profile rating+versioned history+review-state display, outage → truthful unavailable state (never fabricated rows).
- [x] 4.9 GREEN: complete wiring until 4.1/4.3/4.6/4.8 pass. — All green: unit 9/9 (projector.test.js), pgTAP 16/16 (rating_projection.sql) + 87/87 across all 9 suites (zero regressions), Playwright 3/3 new + 10/10 full suite (zero regressions). `npm run lint` clean, `npm run build` succeeds.

## Phase 5: Legacy Migration Controls

- [x] 5.1 `src/config/featureFlags.js`: environment-scoped, observable adapter flags (`identity`, `tournaments`, `ratings`), default off in production until verified. — Scope note (see docs/legacy-retirement.md "Known deferred work"): `identity`/`tournaments`/`ratings` are unconditionally `true` because Phases 2-4 already hard-wired all three directly to Supabase with zero fixture fallback in any build — there is nothing left for those three to gate. The one flag with real behavior to gate is `demoFinancialUI` (`resolveDemoFinancialUIFlag`), hard-forced off whenever `import.meta.env.PROD` is true regardless of any env override. `npx vitest run src/config/__tests__/featureFlags.test.js` 6/6 pass.
- [x] 5.2 Environment-selected repository adapters: `src/repositories/*Repository.js` route Supabase vs `src/data/*` fixture per flag; production build asserts no fixture fallback. — Revised scope (see docs/legacy-retirement.md "Resolved in a follow-up batch"): `identity`/`tournaments`/`ratings` still have zero fixture counterpart (unchanged since 5.1), so there is no fixture to route *to*; what closed the real spec gap instead is that these three flags are now genuinely reversible (`resolveAdapterFlag()`, env-scoped, honored in every environment including production) and every repository's `assertConfigured()` now goes through the shared `src/repositories/adapterAvailability.js`, which checks the flag before the dependency and always fails into the same truthful `UNAVAILABLE` state — never a fixture. Verified: `npx vitest run` 73/73 pass, `npm run lint` clean, `npm run build` succeeds, `npx playwright test` 14/14 pass (zero regressions).
- [x] 5.3 RED unit: production flag + dependency outage → truthful unavailable state, never silent fixture fallback. — `src/repositories/__tests__/adapterAvailability.test.js` (6 tests): dependency outage and a disabled adapter flag both throw the same `AppError('UNAVAILABLE', ...)`, flag is checked before dependency, and each path records the correct migration-event kind. `src/config/__tests__/featureFlags.test.js` extended with `resolveAdapterFlag` coverage (default-on, explicit-`'false'`-only disablement).
- [x] 5.4 Migration `0014_migration_audit.sql`: append-only `migration_events` table for flag changes/adapter errors/rollback/reconciliation. — Table + `record_migration_event` SECURITY DEFINER RPC (admin-read-only, no UPDATE/DELETE grant to any client role; anon may call the RPC since an adapter, including identity itself, can fail before any session exists). Real bug found+fixed during pgTAP verification: the new table needed its own explicit `revoke all ... from anon, authenticated` before granting back `select` to `authenticated` — Supabase's schema-level default privileges otherwise auto-grant `ALL` to `anon`/`authenticated` on any new table (the same fact 0004's own comment documents for 0001-0003's tables), which the RED pgTAP run caught directly (anon could read the table, and even the admin role could update/delete it, until the explicit revoke was added). `src/repositories/migrationEventRepository.js` is the client-side best-effort writer, wired into every repository via `adapterAvailability.js`.
- [x] 5.5 `docs/legacy-retirement.md`: per-module consumer checklist gating deletion of `src/data/*`, wallet/market/prediction modules. — Per-module consumer/replacement/status table for every `src/data/*` module, both mock stores, and the prediction/market UI, plus an explicit "Known deferred work" section covering 5.2/5.3/5.4 and a newly-discovered `Navbar.jsx`/`HomePage.jsx` mock-session-store gap (documented, not fixed, in this batch).
- [x] 5.6 Isolate `src/store/useWalletStore.js` and market/prediction modules behind an explicit non-production demo flag; unreachable in production build. — `/wallet` and `/mercados/:id` resolve to `NotFoundPage` outside `FEATURE_FLAGS.demoFinancialUI` (routes stay registered so `RequireAuth`'s own auth-guard on `/wallet` is unaffected — auth check still runs first); `Navbar.jsx`'s wallet-balance link and `HomePage.jsx`'s markets section/betting hero copy are gated the same way. `HomePage.jsx`'s featured-tournaments section was also rewired off the `TOURNAMENTS` fixture onto the real `listTournaments()` repository call as part of this same change (it was still fixture-backed going into this batch). No file deleted — only reachability changed, per "never delete legacy modules in one step".
- [x] 5.7 RED Playwright `e2e/no-financial-ui.spec.js`: production build exposes no wallet/market/prediction routes or components. — 4 tests against a real `npm run build` (no demo-flag override — `import.meta.env.PROD` alone forces it off) + `npm run preview`: `/wallet` and `/mercados/:id` both 404, navbar never shows a wallet link, home page shows no markets section or betting copy. One real regression caught and fixed during verification: an existing batch-2 test (`auth-onboarding.spec.js`, "unauthenticated visitor to a protected route is redirected to /login") used `/wallet` as its example protected route and broke when the route was first removed entirely in production; fixed by keeping `/wallet` always registered under `RequireAuth` and only swapping its *element* (`WalletPage` vs `NotFoundPage`) on the flag, so the auth guard's own redirect behavior is preserved independently of the financial-UI gate. Full Playwright regression: 14/14 pass (10 prior + 4 new, zero regressions).

## Phase 6: Threat-Matrix RED Tests — Application Routing (preserved from design.md)

- [x] 6.1 RED Playwright `e2e/session-routing.spec.js`: token refresh/expiry mid-session redirects correctly. — Real, deterministic technique (no reliance on supabase-js's internal 30s-tick/90s-margin auto-refresh timer, which would be slow/flaky): establishes a real session on a real protected route (`/wallet`), rewrites the *actually persisted* supabase-js session's `expires_at` into the past (discovering the real storage key at runtime rather than hardcoding it), stubs the refresh-token endpoint to fail (`invalid_grant`), then reloads — `getSession()`'s own synchronous expiry check triggers an immediate real refresh attempt, which fails, and `RequireAuth` redirects to `/login`. Passed on first run (not a literal RED→GREEN — the underlying `SessionProvider`/`RequireAuth` 'expired'-state handling was already built in Phase 2) but proven non-vacuous: the pre-expiry assertion (`not.toHaveURL(/login/)`) confirms the session was genuinely valid first, and the storage-key lookup throws loudly if no persisted session is found to corrupt. Verified: `npx vitest run` 80/80 pass, `npm run lint` clean, `npm run build` succeeds, `npx playwright test` 15/15 pass (14 prior + 1 new, zero regressions).
- [x] 6.2 RED Playwright: direct URL to protected/role route without session → `/login`; incomplete profile → `/onboarding`. — 4 tests against the real, newly-wired `/organizador` route (`<RequireAuth role="organizer">`, see 6.3's evidence for why this route exists): anonymous direct URL → `/login` (no flash of protected content); authenticated-no-username (even with a fixture claiming the organizer role) → `/onboarding`, proving RequireAuth resolves the incomplete-profile case before the role check; authenticated-without-role → `/` (not the role-gated page); authenticated organizer with a complete profile → reaches the real page. Verified: `npx playwright test` 19/19 pass (zero regressions).
- [x] 6.3 RED Playwright: forged/stale role claim on protected route → denied server-side regardless of client state. — On `/organizador`: the bootstrap-session stub claims `roles: ['user', 'organizer']` (the forged/stale client-side claim, e.g. a cached grant that no longer matches reality), which is enough to pass `RequireAuth`'s client-side route gate, but the `advance_tournament_state` RPC stub returns the exact `42501 FORBIDDEN` shape `0008_lifecycle_rpc.sql` really raises for an unauthorized caller (already proven server-side by `supabase/tests/lifecycle_rpc.sql`'s "unauthorized denial" pgTAP assertion). Asserts the UI surfaces the real denial message and the tournament's status never optimistically advances — no fabricated success. Verified: `npx playwright test` 20/20 pass (zero regressions), `npx vitest run` 80/80 pass, `npm run lint` clean.
- [x] 6.4 RED Playwright: hostile/forged redirect target → rejected; only allowlisted same-origin destinations honored, no loop. — 4 tests using `page.addInitScript()` + `history.replaceState()` to inject a forged `location.state.from` before the app boots (the only realistic way this state could carry an attacker-controlled value, since a plain external link cannot set another origin's `history.state`): absolute cross-origin URL rejected, protocol-relative host rejected, self-referential `/login` rejected (no loop), and a legitimate same-origin path IS honored (proving the allowlist doesn't just always fall back to `/`). Real behavior discovered and documented while writing this (not a shortcut worked around): supabase-js's implicit-flow hash detection clears `#access_token=...` via a plain `location.hash = ''` assignment, which per spec creates a *new* history entry with `state: null` — wiping any injected `usr.from` before `LoginPage` can read it. This means the test establishes the session first via the hash technique (no forged state involved), then navigates to a hash-less `/login` with the forged/legitimate state — the actually-reachable path for this mechanism. The same finding also confirms a real Google OAuth round-trip can never deliver a non-null `location.state.from` to `LoginPage` either (see the Batch 5 log). `src/pages/LoginPage.jsx`'s `resolveSafeRedirectPath()` (also unit-tested 7/7) is the real, load-bearing allowlist enforcing this. Verified: `npx playwright test` 24/24 pass (zero regressions), `npx vitest run` 80/80 pass, `npm run lint` clean.
- [x] 6.5 GREEN: `src/components/RequireAuth.jsx`/router guards satisfy 6.1-6.4. — Closing checkpoint: `e2e/session-routing.spec.js` (10 tests covering 6.1-6.4) all pass against real, unmodified `RequireAuth.jsx` plus the two pieces of real supporting code this phase added (`src/pages/OrganizerPanelPage.jsx` + its `<RequireAuth role="organizer">` route, and `LoginPage.jsx`'s `resolveSafeRedirectPath()`). No weakened assertions, no synthetic test-only routes — `/organizador` and its role gate are real production surface. Full regression: `npx playwright test` 24/24 pass (14 pre-Phase-6 + 10 new), `npx vitest run` 80/80 pass, `npm run lint` clean, `npm run build` succeeds with stub Supabase env vars. Phase 6 is complete: 6.1-6.5 all done and verified.

## Phase 7: CI, Docs, Cleanup

- [x] 7.1 Finalize CI gate order in `.github/workflows/ci.yml`: frozen install → lint → unit+coverage → Supabase migration/RLS/pgTAP → build → Playwright → `npm audit`. — Rechained every job's `needs` into one strict linear sequence matching this exact order (previously `unit-test`/`db-migrations`/`audit` ran in parallel off `install` directly, and `build`/`e2e` had multi-parent fan-in) — a cheap failure now always short-circuits every more expensive step after it, including the dependency audit, which previously could pass or fail independently of the rest of the pipeline. Verified: YAML parses cleanly (`python3 -c "import yaml..."`) and the resulting `needs` graph is exactly `install→lint→unit-test→db-migrations→build→e2e→audit` with no cycles. Not run end-to-end on real GitHub Actions infrastructure from this environment (no `act`/CI runner available here) — the individual commands each job runs (`npm run lint`, `npx vitest run --coverage`, migration replay + pgTAP, `npm run build`, `npx playwright test`, `npm audit`) are each independently verified throughout this and prior batches' evidence.
- [x] 7.2 `.env.example`, `README.md`: environment/secrets contract, migration/rollback runbook, admin-bootstrap procedure reference (1.2). — `README.md` replaced (was still the default Vite template) with: a client/server/deploy-time environment-variable contract table (cross-checked against every real `import.meta.env.VITE_*`/`Deno.env.get(...)` reference in the codebase, not assumed), a testing-layer summary matching the real `package.json` scripts, a migration/rollback runbook (additive migrations, forward-fix over `DROP`, adapter-flag-based incident rollback via `VITE_FEATURE_*`, `migration_events` audit trail reference), and the admin-bootstrap procedure referencing `0002_admin_bootstrap.sql`/task 1.2. `.env.example` itself: per orchestrator confirmation, the sandbox's `.env*` deny-list blocks read/verification access for both this agent and the orchestrator equally, so its exact-contents verification (and task 0.3) is explicitly left for the user to confirm directly — not re-attempted here.
- [x] 7.3 Verify `proposal.md` success-criteria checklist end-to-end against implemented flows. — At the time this task ran, all 3 criteria were checked off in `proposal.md` with inline evidence; 2 real gaps found and closed, 1 honest caveat recorded (not silently claimed done). **Post-verify correction**: the subsequent `sdd-verify` pass found a CRITICAL gap this task missed (no application-level tournament-creation path — see Phase 8 below), so criterion 2 was reopened/unchecked in `proposal.md` after this task ran. This task's own evidence below is otherwise unaffected — the register→leaderboard chain genuinely works once a tournament exists, it just cannot be reached from a genuinely empty database yet. (a) **closed** — the "register" step of the sign-in→leaderboard chain had zero Playwright coverage (only pgTAP/unit); extended `e2e/tournament-flow.spec.js`'s happy-path test to register a participant before the organizer flow, so the full chain is now e2e-verified end-to-end in one test. (b) **closed** — `src/lib/prediction.js` was an open "not audited" item in `docs/legacy-retirement.md`; audited its full consumer chain (`PredictionWidget.jsx` → `MarketDetailPage.jsx` → route resolves to `NotFoundPage` outside `FEATURE_FLAGS.demoFinancialUI`) and confirmed no non-gated path reaches it. (c) **caveat, left honest not silently checked** — "alerts" (from criterion 3's "errors, audit, logs, and alerts") is not implemented: structured logs/audit exist but nothing pages/notifies an operator; this is an ops-platform decision tied to design.md's still-open "which deployment host" question, not a code gap. Also fixed a stale documentation inconsistency while verifying: `design.md`'s two "Blocking" Open Questions (launch game/format, admin bootstrap) were resolved back in Phases 1/3 (see `tasks.md`'s own "Resolved Decisions" header) but were never checked off in `design.md` itself — checked off now with references to where each was actually implemented. Verified: `npx playwright test` 24/24 pass, `npx vitest run` 80/80 pass, `npm run lint` clean, `npm run build` succeeds, no service-role/secret leakage in `src/` (grep-verified).

## Phase 8: Deferred to Follow-Up (Completed, 2026-08-12)

- [x] 8.1 Migration: `create_tournament` RPC (organizer/admin-authorized, matching the same `request_id`-idempotent/`command_outcomes`-audited pattern as every other write RPC in this change — see `0008_lifecycle_rpc.sql`/`0009_registration_rpc.sql` for the established shape). Created `0015_create_tournament_rpc.sql` with `create_tournament` and `claim_organizer_role` RPCs.
- [x] 8.2 `src/repositories/tournamentRepository.js`: added `createTournament` and `claimOrganizerRole` functions calling the new RPCs.
- [x] 8.3 Minimal UI entry point — updated `src/pages/OrganizerPanelPage.jsx` with a tournament creation form and button.
- [x] 8.4 pgTAP: created `supabase/tests/create_tournament.sql` proving authorized creation succeeds, is idempotent on retry, records audit, and denies unauthorized callers (5/5 assertions pass).
- [x] 8.5 Playwright: extended `e2e/tournament-flow.spec.js` with an E2E test proving an authenticated organizer creates a tournament from zero on `/organizador`.
- [x] 8.6 Checked off `proposal.md`'s success criterion 2.

### Batch 6 (Phase 8 — Tournament Creation from Zero)

- Completed and verified: all of Phase 8 (8.1-8.6). Cumulative: 72/73 tasks (all original tasks 0.1-7.3 except 0.3 + all Phase 8 tasks 8.1-8.6).
- New SQL: `supabase/migrations/0015_create_tournament_rpc.sql` (`create_tournament` and `claim_organizer_role` SECURITY DEFINER RPCs + grants).
- New repository methods: `createTournament` and `claimOrganizerRole` in `src/repositories/tournamentRepository.js`.
- Rewired UI: `src/pages/OrganizerPanelPage.jsx` updated with tournament creation form.
- New pgTAP suite: `supabase/tests/create_tournament.sql` (5 assertions, 100% pass).
- New E2E spec: extended `e2e/tournament-flow.spec.js` with tournament creation from zero.
- Verification: Postgres Docker container migration replay (migrations 0000-0015) + 11 pgTAP test suites (104 assertions pass); `npx vitest run` 80/80 pass; `npm run lint` clean; `npm run build` succeeds; Playwright e2e 25/25 pass.


## Apply Progress Log

### Batch 1 (this pass)

- Completed and verified: Phase 0 (0.1, 0.2, 0.4, 0.5) and Phase 1 (1.1-1.11) — 15/66 tasks.
- Partial: 0.3 — `supabase/config.toml` done; `.env.example` blocked by the sandbox's global `.env*` write deny-list (see note on 0.3). Not a design decision — needs a maintainer with unrestricted file access, or the deny-list scoped to allow this one placeholder-only file.
- Verification method: real `supabase/postgres:15.8.1.049` Docker container (not the full `supabase start` stack, to keep this batch's runtime footprint small) — migrations 0000-0005 applied via sequential `psql -f` replay in filename order (equivalent to what `supabase db reset` does), then both Phase 1 pgTAP suites run against that same container. Container removed after verification (`docker rm -f gg2-pg-test`); no residual state.
- `gentle-ai sdd-attempt` runtime-authority: one attempt acquired/settled for this batch, outcome `passed`, but flagged `changed_line_budget_exceeded: true` (attempt-level `max-changed-lines 2000` vs. this batch's actual ~4036 changed lines) with `decision_required: true` / `next_action: reset`. This is the native runtime-attempt budget accounting, separate from and in addition to the SDD `delivery_strategy: exception-ok` already accepted by the maintainer for this whole change. A follow-up apply batch should acquire a fresh attempt with a larger `--max-changed-lines` (or expect the same flag and treat it as informational, since the change-level `size:exception` is already resolved).
- Next batch should resume at Phase 2 (Authenticated Identity), starting with 2.1.

### Batch 2 (this pass)

- Completed and verified: all of Phase 2 (Authenticated Identity — 2.1-2.11). Cumulative: 26/66 tasks (batch 1's 15 + batch 2's 11).
- 0.3 remains partial/unchecked exactly as batch 1 left it — not attempted again this batch per explicit instruction; still blocked by the sandbox's global `.env*` deny-list.
- Scope decision (deviation worth flagging, not a silent one): `src/store/useSessionStore.js` and its remaining wallet/demo consumers (`Navbar.jsx`, `WalletPage.jsx`, `BuySharesPanel.jsx`, `HomePage.jsx`) were deliberately left untouched per the "never delete/migrate legacy modules in one step" instruction and Phase 2's scope (identity only). `LoginPage`/`OnboardingUsernamePage`/`RequireAuth` now run on the real `SessionProvider`, not the mock store, so those legacy wallet-flow pages will show a logged-out mock user until Phase 5 (`legacy-migration-controls`) migrates or flags them off — they were already slated for non-production isolation there, and Stage 1 explicitly excludes the wallet/financial UI they render, so this is not a functional regression in anything Stage 1 claims to deliver.
- Two real bugs found and fixed via the RED tests themselves (not theoretical — both reproduced against a live container/browser before the fix):
  1. pgTAP: `reset role` does not clear a `set local "request.jwt.claim.sub"` GUC from a prior test block, so a later "unauthenticated" assertion silently inherited the previous user's identity. Fixed by explicitly clearing the GUC (`set local "request.jwt.claim.sub" = '';`).
  2. `claim_username`: a `raise exception` for a later validation/throttle failure was rolling back an earlier, already-completed `insert` from the same top-level function call (PL/pgSQL savepoint semantics — the nearest enclosing exception handler, in the caller, rolls back everything since its own savepoint). Fixed by never raising for expected business outcomes (claimed/conflict/rate_limited all return a structured result instead), so nothing after the format-validation gate can roll back the throttle/audit bookkeeping.
  3. App-level: a real Google OAuth redirect returns the browser to `/` (not wherever "sign in" was clicked), so a freshly authenticated, still-username-less session had no route ever redirecting it to `/onboarding`. Found by the Playwright e2e run itself (RED), fixed with a global `OnboardingRedirect` effect in `App.jsx`.
  4. CI/tooling: `vitest.config.js` had no `exclude` for `e2e/**`, so once a real Playwright spec file existed, Vitest's default include glob picked it up and crashed on Playwright's `test.describe`. Also, `.github/workflows/ci.yml`'s `e2e` job ran `npx playwright test` (which serves `dist/` via `npm run preview`) without ever running `npm run build` in that job — invisible while there were 0 spec files, but would have made every future e2e run fail. Both fixed.
- Verification method: (a) unit — `npx vitest run` 15/15 pass (5 new `SessionProvider` tests + the 10 from batch 1); `npm run lint` clean; `npm run build` succeeds both with and without stub `VITE_SUPABASE_*` env vars. (b) database — `supabase/postgres:15.8.1.049` Docker container, migrations 0000-0006 replayed via sequential `psql -f`, all three pgTAP suites re-run (`rls_deny_by_default` 8/8, `admin_bootstrap` 4/4, `username_claim_race` 11/11, zero regressions), container removed after (`docker rm -f gg2-pg-test`). (c) e2e — real `npm run build` (stub `VITE_SUPABASE_URL=https://e2e-stub.supabase.co`, `VITE_SUPABASE_ANON_KEY=e2e-stub-anon-key`, never a real secret) + `npm run preview`, then `npx playwright test e2e/auth-onboarding.spec.js` (Chromium installed fresh this session): 5/5 pass, covering fresh-sign-in→onboarding, username collision, throttle, successful claim→protected-route access, and unauthenticated→`/login` redirect.
- `gentle-ai sdd-attempt` runtime-authority: prior objective's `decision_required` was resolved via `sdd-attempt reset` (explicit reason recorded), then a fresh attempt was acquired with `--max-changed-lines 8000` per instruction, and settled `outcome: passed`, `state: complete` — no budget flag this time.
- Next batch should resume at Phase 3 (Tournament Operations — SSBU, single-elimination, 8 participants), starting with 3.1.

### Batch 3 (this pass)

- Completed and verified: all of Phase 3 (Tournament Operations — 3.1-3.15, plus one unplanned 3.16 fix found during wiring). Cumulative: 41/66 tasks (batch 1's 15 + batch 2's 11 + batch 3's 15).
- 0.3 remains partial/unchecked exactly as batches 1-2 left it — not attempted again this batch (same sandbox `.env*` deny-list, out of this batch's assigned scope).
- New SQL: `supabase/migrations/0007_ssbu_format_seed.sql` (seed), `0008_lifecycle_rpc.sql` (`advance_tournament_state`), `0009_registration_rpc.sql` (`register_participant`/`withdraw_participant`), `0010_bracket_rpc.sql` (`generate_bracket`), `0011_result_rpc.sql` (`submit_official_result`); amended `0004_rls_policies.sql`/`0005_public_views.sql` (see 3.16). New `supabase/seed.sql` (empty by design — the real catalog is a migration, not a fixture).
- New domain logic (pure, unit-tested, client-side mirrors of the real server-side authority): `src/domain/tournaments/{lifecycle,registration,bracket,result}.js`.
- New repositories: `src/repositories/{tournamentRepository,bracketRepository,resultRepository}.js`.
- New Edge Function: `supabase/functions/result-command/index.ts` (same "not directly executable, verified by review + e2e stub" constraint as `bootstrap-session` in batch 2 — no Deno runtime in this environment).
- New UI: `src/pages/{TournamentsPage,TournamentDetailPage}.jsx` rewritten against real repositories; new `src/components/TournamentStatusBadge.jsx`. `src/components/{TournamentCard,StatusBadge,MatchRow}.jsx` deliberately left untouched (still used by `HomePage`/`MarketDetailPage`'s legacy mock-data flow, out of this batch's scope per "never delete/migrate legacy modules in one step" — `MatchRow` is now unused by the real pages but not deleted, a Phase 5 concern).
- New pgTAP: `supabase/tests/{roster_concurrency,bracket_concurrency,lifecycle_rpc,result_submission,public_projection}.sql` (52 new assertions); amended `supabase/tests/rls_deny_by_default.sql` (one `on conflict do nothing` fix, see bug 1 below). New Playwright: `e2e/tournament-flow.spec.js` (2 tests).
- Real bugs found and fixed via the RED verification itself (not theoretical):
  1. `rls_deny_by_default.sql` (pre-existing from batch 1) unconditionally inserted a `games` row with id `'ssbu'`, which now collides with 3.1's seed migration — every run after 0007 applied would abort the whole pgTAP transaction. Fixed with `on conflict (id) do nothing`.
  2. `generate_bracket`: round-1 match inserts referenced round-2/final match ids via `next_match_id` before those rows existed, violating the FK (`matches_next_match_id_fkey`). Fixed by inserting leaf-to-root (final → round 2 → round 1) so every foreign key already points at an existing row.
  3. Architectural gap in `public_brackets_view`/`public_leaderboard_view`/`public_player_history_view` (all from batch 1's Phase 1, not previously caught because 1.6's RLS test only covered profiles/memberships/audit/command_outcomes): `security_invoker = true` on a view joining RLS-private tables (`memberships`, `profiles`, `rating_events`, `rating_snapshots`) made those views literally inaccessible to `anon`/non-participants — a permission error, not an empty/curated result — silently failing the tournament-operations "Public projection and closed perimeter" requirement. First attempted fix (granting `anon` direct table access + "published" RLS policies on `brackets`/`matches`/`results`) was itself wrong and reverted: it would let `anon` bypass the curated view entirely via raw PostgREST table queries, violating 0004's own stated "public reads go through the views, never these base tables" convention. Correct fix: remove `security_invoker` so the views run as their owner (bypassing RLS on the joined tables, same pattern as every SECURITY DEFINER RPC), with the view's own column allowlist and `where status <> 'DRAFT'` filter as the actual security boundary. Proven by new `public_projection.sql` (7/7).
  4. Playwright `tournament-flow.spec.js` outage test: a `route.continue()` guard for non-GET methods on the mocked `tournaments` endpoint forwarded the real CORS preflight OPTIONS request to the nonexistent stub host, hanging the page in a permanent loading state. Removed the guard (the route only ever legitimately receives GET in this app). Separately, supabase-js's built-in retry-with-backoff on a 503 GET response (up to ~7s across retries) is real, correct client resilience behavior, not a bug — the test's assertion timeout was extended to 20s to match it rather than papering over it by disabling retries.
- Verification method: (a) unit — `npx vitest run` 49/49 pass (34 new domain tests: 10 lifecycle + 8 registration + 6 bracket + 10 result, plus the 15 from batches 1-2); `npm run lint` clean (only the pre-existing `SessionProvider.jsx` fast-refresh warning); `npm run build` succeeds with stub `VITE_SUPABASE_*` env vars. (b) database — `supabase/postgres:15.8.1.049` Docker container, migrations 0000-0011 replayed via sequential `psql -f` from a clean container (twice, after the 0004/0005 fix), all 8 pgTAP suites run together in the same replay (`rls_deny_by_default` 8/8, `admin_bootstrap` 4/4, `username_claim_race` 11/11, `roster_concurrency` 13/13, `bracket_concurrency` 11/11, `result_submission` 9/9, `lifecycle_rpc` 8/8, `public_projection` 7/7 — 71 total, zero regressions), container removed after each run (`docker rm -f gg2-pg-test`). (c) e2e — real `npm run build` (stub Supabase env vars) + `npm run preview`, `npx playwright test`: 7/7 pass (5 from batch 2's `auth-onboarding.spec.js` + 2 new `tournament-flow.spec.js`, zero regressions).
- `gentle-ai sdd-attempt` runtime-authority: fresh attempt acquired with `--max-changed-lines 10000` per instruction (work unit `phase3-tournament-operations`); no budget-exceeded flag surfaced during this batch's work.
- Next batch should resume at Phase 4 (Rating Projections), starting with 4.1. Phase 3 is fully complete and verified; nothing was left partial in this phase.

### Batch 4 (this pass)

- Completed and verified: all of Phase 4 (Rating Projections — 4.1-4.9) and a deliberate subset of Phase 5 (Legacy Migration Controls — 5.1, 5.5, 5.6, 5.7; 5.2/5.3/5.4 explicitly deferred with rationale, see below and `docs/legacy-retirement.md`). Cumulative: 55/67 tasks (batch 1's 15 + batch 2's 11 + batch 3's 16 [15 planned + unplanned 3.16] + batch 4's 13).
- Runtime-authority housekeeping: batch 3's `phase3-tournament-operations` attempt was left `running`/unsettled at the start of this batch (an unclosed loop from the prior session, not a real defect); settled it `outcome: passed` with batch 3's own already-documented evidence before acquiring a fresh attempt for this batch's `phase4-rating-projections` work unit (`--max-changed-lines 8000`).
- New SQL: `supabase/migrations/0012_rating_projector_trigger.sql` (`project_rating_event()` AFTER INSERT trigger on `results` + `recompute_rating_snapshot()`, both SECURITY DEFINER; `unique (result_id, participant_membership_id)` constraint on `rating_events` for idempotency), `0013_correction_rpc.sql` (`correct_result` RPC + `results.correction_reason`/`corrected_by` columns). New pgTAP `supabase/tests/rating_projection.sql` (16 assertions).
- New domain logic: `src/domain/ratings/projector.js` (+ `__tests__/projector.test.js`, 9 tests) — pure client-side mirror of the trigger's win/loss delta computation and of `correct_result`'s optimistic-concurrency version-conflict check.
- New repository: `src/repositories/ratingRepository.js` (`getLeaderboard`/`getPlayerRatings`/`getPlayerHistory`, reading `public_leaderboard_view`/`public_player_history_view`). Removed a duplicate, zero-consumer `getPublicPlayerHistory` from `profileRepository.js` (dead scaffold from an earlier phase) in favor of this single canonical location. Added `correctResult` to `resultRepository.js` (direct RPC call, no dedicated UI consumer yet — repository-level completeness only).
- Rewired UI: `src/pages/{LeaderboardPage,PlayerProfilePage}.jsx` off `src/data/players.js` fixtures onto `ratingRepository.js`, matching the established async loading/error/empty pattern. New `e2e/leaderboard-history.spec.js` (3 tests).
- Design choices worth flagging (documented in the migration comments, not silent): (a) the projector is an `AFTER INSERT` trigger, not duplicated logic in each RPC, so both `submit_official_result` (existing) and `correct_result` (new) get identical projection behavior for free; (b) idempotency for the trigger itself is defense-in-depth (`on conflict do nothing` + unique constraint) layered on top of the *real* retry-safety, which is the parent RPC's own `command_outcomes` ledger (a retried request_id never inserts a second `results` row at all, so the trigger can only ever fire once per genuine result); (c) `correct_result` intentionally does not retroactively reflow already-advanced bracket state — documented as an explicit Stage-1 scope boundary (bracket-integrity reflow is a materially larger feature outside rating-projections spec).
- Two real test-authoring bugs found and fixed while verifying `rating_projection.sql` (not theoretical): an RLS-restricted subquery returned a null `result_id` when run as an unauthorized bystander (RLS on `results` hid the row from that role entirely) — fixed by capturing the id once via `\gset` while still running as the superuser test-runner, before any role switch; and psql does not interpolate `:'variables'` inside a `$$ ... $$` dollar-quoted string passed to `throws_ok` — fixed by building that one dynamic query with `format()`/`%L` outside the dollar-quoting instead.
- Phase 5 scope decision (deliberate, not a shortfall): implemented `src/config/featureFlags.js` (5.1), `docs/legacy-retirement.md` (5.5), demo-financial-UI isolation (5.6: `/wallet`/`/mercados/:id` resolve to `NotFoundPage` outside `FEATURE_FLAGS.demoFinancialUI`, which is hard-forced off whenever `import.meta.env.PROD` is true regardless of any env override; `Navbar.jsx`/`HomePage.jsx` financial UI gated the same way; `HomePage.jsx`'s featured-tournaments section also rewired off the `TOURNAMENTS` fixture onto the real `listTournaments()` call, since it was still fixture-backed going into this batch), and `e2e/no-financial-ui.spec.js` (5.7). Deferred 5.2 (dual Supabase/fixture adapter routing for identity/tournaments/ratings), 5.3 (its RED coverage), and 5.4 (`migration_events` audit table) — full rationale in `docs/legacy-retirement.md` "Known deferred work": Phases 2-4 already hard-wired identity/tournaments/ratings directly to Supabase with zero fixture fallback in any build, so there is no real dual-adapter behavior to route yet, and retrofitting one onto three already-verified, production-carrying phases deserves its own focused, fully-regression-tested batch rather than a rushed addition here; the audit table has no real runtime writer yet since every flag today is a build-time constant, not a runtime toggle.
- Real regression found and fixed during Phase-5 verification: removing the `/wallet` route entirely in production (first attempt) broke a pre-existing batch-2 Playwright test (`auth-onboarding.spec.js`, "unauthenticated visitor to a protected route is redirected to /login") which used `/wallet` as its example `RequireAuth`-protected route — the route no longer existed to redirect *from*. Fixed by keeping `/wallet` always registered under `RequireAuth` and only swapping its rendered *element* (`WalletPage` vs `NotFoundPage`) on the flag, so the unrelated auth-guard behavior is preserved independently of the financial-UI gate.
- New discovery, documented not fixed (out of this batch's declared scope, tracked in `docs/legacy-retirement.md`): `Navbar.jsx` and `HomePage.jsx`'s "Continuar con Google" CTA still read the legacy mock `useSessionStore`, not the real `SessionProvider` — a real Google sign-in currently leaves the navbar looking logged out. Not a fabricated-identity or financial-state bug (the mock store's own login mutator is never called from any real code path anymore), but a real UX gap a follow-up batch should fix alongside 5.2.
- Verification method: (a) unit — `npx vitest run` 64/64 pass (9 new `src/domain/ratings/__tests__/projector.test.js` + 6 new `src/config/__tests__/featureFlags.test.js`, plus the 49 from batches 1-3, zero regressions); `npm run lint` clean (only the pre-existing `SessionProvider.jsx` fast-refresh warning); `npm run build` succeeds with stub `VITE_SUPABASE_*` env vars and no demo-flag override. (b) database — `supabase/postgres:15.8.1.049` Docker container, migrations 0000-0013 replayed via sequential `psql -f` from a clean container (twice — once mid-batch, once as a final full re-verification), all 9 pgTAP suites run together in the same replay (`rls_deny_by_default` 8/8, `admin_bootstrap` 4/4, `username_claim_race` 11/11, `roster_concurrency` 13/13, `bracket_concurrency` 11/11, `result_submission` 9/9, `lifecycle_rpc` 8/8, `public_projection` 7/7, `rating_projection` 16/16 — 87 total, zero regressions), container removed after each run (`docker rm -f gg2-pg-test`). (c) e2e — real `npm run build` (stub Supabase env vars, no demo-flag override, so this is a genuine production build) + `npm run preview`, `npx playwright test`: 14/14 pass (10 from batches 2-3 + 3 new `leaderboard-history.spec.js` + 4 new `no-financial-ui.spec.js`, zero regressions after the `/wallet` routing fix above).
- `gentle-ai sdd-attempt` runtime-authority: settled batch 3's leftover `phase3-tournament-operations` attempt (`outcome: passed`), then acquired and settled a fresh `phase4-rating-projections` attempt (`--max-changed-lines 8000`, `outcome: passed`); no budget-exceeded flag surfaced.
- Next batch should resume at Phase 5, task 5.2 (dual-adapter routing for identity/tournaments/ratings + its RED coverage + the migration-audit table), then continue into Phase 6 (Threat-Matrix RED Tests — Application Routing) and Phase 7 (CI, Docs, Cleanup). Also worth picking up: the `Navbar.jsx`/`HomePage.jsx` mock-session-store gap noted above, and auditing `src/lib/prediction.js` consumers per `docs/legacy-retirement.md`'s "Known deferred work".

### Batch 5 (this pass — multiple checkpoints due to repeated environment interruptions)

This batch's session was interrupted by the host process several times mid-work; each interruption is why this entry has multiple internal checkpoints rather than one final summary. Cumulative: 59/67 tasks (55 going in + 5.2/5.3/5.4/6.1 this batch).

**Phase 5 remainder (5.2, 5.3, 5.4) — complete and verified:**
- Closed both real spec gaps a prior validator flagged in the Phase 5 deferral (see `docs/legacy-retirement.md` "Resolved in a follow-up batch"): (a) `identity`/`tournaments`/`ratings` are now genuinely reversible env-scoped flags (`resolveAdapterFlag()` in `featureFlags.js`, `VITE_FEATURE_IDENTITY`/`VITE_FEATURE_TOURNAMENTS`/`VITE_FEATURE_RATINGS`, honored in every environment including production — an operator can really disable one now); (b) `0014_migration_audit.sql` adds the append-only `migration_events` table + `record_migration_event` SECURITY DEFINER RPC, and `src/repositories/migrationEventRepository.js` is the client-side best-effort writer, wired into every repository via the new shared `src/repositories/adapterAvailability.js` (`assertConfigured()` in every repository now goes through this one shared helper).
- Still no fixture dual-adapter for identity/tournaments/ratings, and none was needed: these three still have zero fixture counterpart, so the "safe path" on disable is still the truthful `UNAVAILABLE` state, not a fixture — this was the deferral's original, still-correct reasoning; only the *reversibility* and *audit trail* gaps needed closing.
- Real bug found+fixed via the RED pgTAP run for `0014`: the new `migration_events` table needed its own explicit `revoke all on ... from anon, authenticated` before granting back `select` to `authenticated` — Supabase's schema-level default privileges auto-grant `ALL` to `anon`/`authenticated` on any new table (the same fact 0004's own comment already documented for 0001-0003's tables, but I missed it for this new table on the first pass). The RED test caught it directly: anon could read the table, and even the admin role could update/delete it, until the explicit revoke was added. Also caught and fixed a test-harness-only issue in my own verification process (not a product bug): creating the `pgtap` extension *before* replaying migrations makes `public.has_role('admin')` ambiguously resolve against pgTAP's own same-named `has_role` function inside `CREATE POLICY` statements — migrations must be replayed before `pgtap` is installed, exactly as prior batches already did.
- New files: `supabase/migrations/0014_migration_audit.sql`, `supabase/tests/migration_audit.sql` (12 assertions), `src/repositories/{migrationEventRepository,adapterAvailability}.js`, `src/repositories/__tests__/adapterAvailability.test.js` (6 tests). Modified: `src/config/featureFlags.js` (+ its test file), all six Supabase-backed repositories (`session`, `profile`, `tournament`, `bracket`, `result`, `rating`) now delegate `assertConfigured()` to `adapterAvailability.js`.
- Verification: migrations 0000-0014 replayed via sequential `psql -f` against a clean `supabase/postgres:15.8.1.049` container (exit 0 each, correct order: migrations first, then `pgtap`), all 10 pgTAP suites 99/99 `ok` (87 prior + 12 new, zero regressions), container removed after (`docker rm -f gg2-pg-test`); `npx vitest run` 73/73 pass; `npm run lint` clean; `npm run build` succeeds with stub Supabase env vars; `npx playwright test` 14/14 pass (zero regressions).
- Checkpoint discipline: settled the `phase5-6-legacy-and-routing` runtime attempt for exactly this Phase-5-remainder slice (`outcome: passed`) before starting Phase 6, per the confirmed self-reset/checkpoint-per-phase policy, rather than carrying one open attempt across the whole batch.

**Phase 6 (routing threat matrix) — 6.1 complete and verified; 6.2-6.5 have real supporting code on disk but no dedicated tests yet, not checked off:**
- 6.1 (see its own task-line evidence above) is done and verified.
- In the course of designing 6.1-6.4 together (before an interruption forced narrowing to 6.1 only), three pieces of real, load-bearing infrastructure were already built and are sitting on disk, verified only incidentally (full suite green, no regressions) but not yet exercised by their own dedicated RED/GREEN tests for 6.2-6.4:
  1. `src/pages/OrganizerPanelPage.jsx` + its route in `App.jsx` (`/organizador`, wrapped in `<RequireAuth role="organizer">`) — the first real production usage of `RequireAuth`'s `role` prop (built in Phase 2, unused by any route until now). Lists the current user's own tournaments and lets them close registration on one via the already-verified `advanceTournamentState` RPC call; a server-side `FORBIDDEN` denial (e.g. from a forged/stale client-side role claim) surfaces as a real error message rather than a fabricated success.
  2. `src/pages/LoginPage.jsx`'s new `resolveSafeRedirectPath()` (exported, unit-tested 7/7 in `src/pages/__tests__/LoginPage.test.js`) — allowlists the post-login return destination to an in-app, same-origin, absolute relative path, rejecting absolute cross-origin URLs, protocol-relative hosts, non-http(s) schemes, and a redirect back into `/login` itself (loop prevention). This closes a real (if narrow — genuinely exploitable only with an XSS-level or hostile-prior-page precondition, not a plain external link) open-redirect gap defensively, matching design.md's threat matrix "Allowlisted same-origin returns prevent loops/open redirects" requirement.
  3. A worthwhile finding from this investigation, documented here since it belongs with 6.4's eventual E2E test: a real Google OAuth round-trip always returns to `window.location.origin` (i.e. `/`, never wherever the user clicked "sign in" from), which means `LoginPage`'s `location.state.from` return-to-origin mechanism is effectively unreachable for a *real* OAuth sign-in — it only ever fires for a client-side-only re-authentication while still mounted on `/login` (e.g. an already-valid persisted session resolving after a same-tab SPA redirect). The 6.4 E2E test still needs writing; it should use `page.addInitScript()` + `history.replaceState()` to inject a forged `state.usr.from.pathname` before the app boots (the technique investigated but not yet written into a spec file), since a plain crafted link cannot set another origin's `history.state` on its own.
- None of 6.2/6.3/6.4/6.5 are checked off — only the supporting production code exists; the dedicated Playwright assertions proving each threat scenario do not exist yet in `e2e/session-routing.spec.js`.
- Checkpoint discipline: acquired a fresh `phase6-routing-threat-matrix` runtime attempt after settling Phase 5's; this narrower 6.1-only increment was settled on its own rather than left open, per the coordinator's explicit "checkpoint after every single task, not just every phase" instruction given how frequently this session was being interrupted.
- Next continuation should resume Phase 6 at 6.2 (direct URL to `/organizador` unauthenticated → `/login`, authenticated-no-username → `/onboarding` — both exercisable today against the real `/organizador` route already on disk), then 6.3 (forged/stale role claim on `/organizador` → the `advance_tournament_state` RPC stub returns `FORBIDDEN`, UI must show real denial, not fabricated success — `OrganizerPanelPage.jsx` already renders this path, just needs a test), then 6.4 (hostile redirect target — use the `addInitScript`/`history.replaceState` technique described above against `resolveSafeRedirectPath`, which is already implemented and unit-tested), then 6.5 (GREEN checkpoint once 6.1-6.4 all pass), then Phase 7 if time remains (note: `.env.example` exists at repo root, created outside the sandbox's deny-list by the maintainer directly, but this batch's sandbox still denies Read/Bash access to that exact path — a continuation with unrestricted file access should verify its contents against the documented env-var contract and check off 0.3/7.2 together).

**Continuation (same Batch 5 session, no further interruptions) — Phase 6 completed (6.2-6.5), Phase 7 completed (7.1-7.3). Final cumulative: 66/67 tasks — every task checked off except 0.3, deferred to the user by design (see 0.3's own line above).**

- **6.2** (4 tests) and **6.3** (1 test) added to `e2e/session-routing.spec.js` against the real `/organizador` route and `advance_tournament_state` RPC exactly as planned above — no surprises, both passed on the first real run.
- **6.4** (4 tests) required redesigning the planned technique after a genuinely useful discovery: the originally-planned single-navigation approach (forge `history.state` via `addInitScript`, then `goto('/login' + accessTokenHash)` in one step) produced **vacuously-passing** tests — every case, including the "legitimate destination is honored" positive case, landed on `/` regardless of the forged input, because supabase-js's implicit-flow hash detection clears `#access_token=...` via a plain `location.hash = ''` assignment, and per the URL/history spec that fragment-only navigation creates a *new* history entry with `state: null` (not inherited from the previous entry) — wiping the injected `usr.from` before `LoginPage`'s effect could ever read it. Caught this by writing a disposable debug spec (`e2e/debug-state.spec.js`, deleted before finishing) that logged `window.history.state` at each step rather than trusting the first green run. Fixed by splitting into two steps: establish a real session first via the hash technique (no forged state involved), then navigate to a hash-less `/login` with the forged/legitimate state — the only path this mechanism is actually reachable through in this app (matching the same finding already logged for 6.1/the OAuth-round-trip: real Google sign-in also can never deliver a non-null `location.state.from`, since it always returns to `/`, not `/login`). All 4 tests then passed genuinely, including the positive "legitimate `/ranking` destination is honored" case that had been silently false before the fix.
- **6.5** closed Phase 6 as a pure verification checkpoint (no new code) once 6.1-6.4 were all green together: 24/24 Playwright, 80/80 vitest, lint clean, build succeeds.
- **7.1**: rechained every CI job's `needs` in `.github/workflows/ci.yml` into one strict linear sequence (`install→lint→unit-test→db-migrations→build→e2e→audit`) matching the exact order named in the task — previously `unit-test`/`db-migrations`/`audit` ran in parallel off `install` directly and `audit` could pass/fail independently of the rest of the pipeline. Verified via `python3 -c "import yaml..."` (valid YAML, correct linear graph, no cycles) — not run against real GitHub Actions infrastructure from this sandbox (no runner available); each job's underlying command is independently verified throughout this and prior batches.
- **7.2**: `README.md` (still the default Vite template going into this batch) replaced with a real environment/secrets contract (cross-checked against every actual `import.meta.env.VITE_*`/`Deno.env.get(...)` reference in the codebase, not assumed), a testing-layer summary matching real `package.json` scripts, a migration/rollback runbook, and the admin-bootstrap procedure referencing `0002_admin_bootstrap.sql`. `.env.example` content-verification explicitly left for the user per the orchestrator's correction (the deny-list blocks both agent and orchestrator equally) — not re-attempted.
- **7.3**: verified `proposal.md`'s three success criteria end-to-end and found two real, previously-undetected gaps, both closed rather than just documented: (a) the "register" step of the sign-in→leaderboard flow had zero Playwright coverage (only pgTAP/unit) despite the proposal describing one continuous chain — extended `e2e/tournament-flow.spec.js`'s existing happy-path test to register a participant first, so the full chain is now e2e-verified in one test; (b) `src/lib/prediction.js` was an open "not audited" item in `docs/legacy-retirement.md` — audited its full consumer chain and confirmed every path is contained behind `FEATURE_FLAGS.demoFinancialUI`, which is hard-forced off in production. Also found and left as an **honest, unchecked caveat rather than a false claim**: "alerts" (named explicitly in success criterion 3) is not implemented anywhere — structured logs/audit exist but nothing pages/notifies an operator; this is an ops-platform decision tied to design.md's still-open "which deployment host" question, not a code gap. Separately fixed a stale documentation inconsistency: `design.md`'s two "Blocking" Open Questions (launch game/format, admin bootstrap) were resolved back in Phases 1/3 but were never checked off in `design.md` itself — checked off now with pointers to where each was actually implemented.
- Real bugs/gaps found and fixed in this continuation (not theoretical): (1) the vacuous-6.4-test bug described above — caught by disposable debug tooling rather than trusting a first green run, a direct instance of the TDD skill's "tests that pass on the first run may not be testing what you think"; (2) the missing e2e coverage for tournament registration; (3) the `prediction.js` audit gap; (4) the CI `needs` graph allowing `audit` and expensive jobs to run out of fail-fast order; (5) the stale unchecked `design.md` Open Questions.
- Verification method for this continuation: `npx playwright test` 24/24 pass (10 new since 6.1's 15: 4×6.2 + 1×6.3 + 4×6.4, plus tournament-flow.spec.js's existing 2 tests extended, zero net new test count change there since it's the same test file), `npx vitest run` 80/80 pass (unchanged from 6.1's count — no new unit tests added in this continuation), `npm run lint` clean (only the two pre-existing fast-refresh warnings), `npm run build` succeeds with stub Supabase env vars, no service-role/secret leakage in `src/` (grep-verified).
- `gentle-ai sdd-attempt` runtime-authority: one fresh attempt acquired and settled per task from 6.2 through 7.3 (7 settles total in this continuation: 6.2, 6.4+6.5 together, 7.1, 7.2, 7.3), each with real evidence (`git diff | sha256sum` as the evidence-revision, matching the convention established for 6.1's settle), per the coordinator's explicit checkpoint-per-task instruction. No budget-exceeded flags surfaced.

### Orchestrator note (post-apply, pre-verify)

A fresh-context validator reviewing this final batch confirmed `/organizador`'s role gate is safe (real server-side authority via `advance_tournament_state`'s ownership check, not just the client-side `role="organizer"` prop) but flagged a genuine, non-blocking provisioning gap: no migration ever grants the `'organizer'` role value — only `0002_admin_bootstrap.sql` grants `'admin'`, to one fixed email — so `/organizador` is currently unreachable by any legitimate tournament owner through a documented flow. The orchestrator attempted a quick fix (dropping the `role="organizer"` prop, since real authority is ownership-based) but caught before committing that this would break `e2e/session-routing.spec.js`'s 6.2 threat-matrix test, which specifically asserts an authenticated non-organizer is redirected away from this exact route. Reverted; left as a documented follow-up (comments added in `App.jsx`/`OrganizerPanelPage.jsx` pointing here) rather than a rushed fix. A real fix needs an actual organizer-role-granting mechanism (e.g. an admin action, or switching the route's client-side gate to an ownership check instead of a role check) — out of scope for this apply pass.
- This change's Phase 0-7 implementation work is now complete: 66/67 tasks checked off, the sole exception (0.3) blocked on a sandbox constraint that applies equally to this agent and the orchestrator, explicitly left for the user. Recommended next step is `sdd-verify`, not another `sdd-apply` batch.
